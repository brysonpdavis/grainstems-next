import { type Properties } from 'csstype'
import { electricBlue } from '../utils/constants/colors'

export const activeButtonStyles: Properties = { borderColor: electricBlue, boxShadow: '0px 0px 60px -5px' }
